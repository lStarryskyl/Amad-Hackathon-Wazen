{
  "name": "@workspace/api-spec",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "codegen": "orval --config ./orval.config.ts && node scripts/check-export-collisions.mjs && pnpm -w run typecheck:libs"
  },
  "devDependencies": {
    "orval": "^8.18.0"
  }
}
